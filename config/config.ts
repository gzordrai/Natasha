import { IConfig } from "../src/interfaces";
import { TOKEN } from "./token";

export const config: IConfig = {
    client: {
        id: "800340983958470666",
        name: "Natasha"
    },
    version: "1.0",
    token: TOKEN
}